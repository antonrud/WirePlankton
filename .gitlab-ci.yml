stages:
    - verify
    - test
    - deploy
    - release

Verify:
    stage: verify
    script:
        - echo $JAVA_HOME
        - mvn verify
    tags:
        - verify

Test:
    stage: test
    script:
        - echo TEST
    tags:
        - test

Deploy:
    stage: deploy
    script:
        - mvn clean deploy
    only:
        - develop
    tags:
        - deploy

Release:
    stage: release
    script:
       - mvn clean deploy
    only:
        - master
    tags:
        - release